version: "3"
services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      - "MYSQL_ROOT_PASSWORD=12345"
    ports:
      - "3306:3306"
    volumes:
      - "C:/Users/ferna/Documents/docker_volumes/mysql:/var/lib/mysql"